package snakeandladder.models;

import snakeandladder.enums.GridType;

public class SnakeGrid extends Grid {
    public SnakeGrid(GridType gridType, int startNo, int endNo) {
        super(gridType, startNo, endNo);
    }
}
